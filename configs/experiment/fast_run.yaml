# @package _global_

dataloader:
  batch_size: 4
  num_workers: 0
  persistent_workers: false

train:
  epochs: 1
  save_interval: 1
  max_steps: 5
  log_interval: 1

wandb:
  mode: offline

paths:
  output_dir: ${paths.project_root}/outputs/fast_run
